{
    "name": "EventFlow Project (Java & Maven)",

    "image": "mcr.microsoft.com/devcontainers/java:0-17-sdk",
    
    // Comandos que serão executados automaticamente DEPOIS que o container for criado.
    "postCreateCommand": "mvn clean install",
    
    // Personalizações para o VS Code dentro do Codespace
    "customizations": {
        "vscode": {
            "extensions": [
                "vscjava.vscode-java-pack"
            ]
        }
    },

    "remoteUser": "vscode"
}
